<% include partials/header %>
<h1>Index</h1>

<% blogs.forEach(function(blog) { %>
    <div>
        <h2><%= blog.title %></h2>
        <img width="400" src="<%= blog.image %>">
        <br>
        <span><%= blog.created %></span>
        <p><%= blog.body.substring(0, 100) %>...</p>
        <a href="/blogs/<%= blog._id%>">Read More</a>
        <hr>
    </div>
<% }); %>

<% include partials/footer %>